SELECT PHOTO
FROM
(
  SELECT PHOTO
  FROM HAKMAIN.SEA013
  WHERE COLREG_NO = :1

  UNION ALL
  
  SELECT  PHOTO
  FROM HAKMAIN.SEB013
  WHERE COLREG_NO = :2
  
  UNION ALL
  
  SELECT PICTURE
  FROM ADMAIN.AIA010
  WHERE EMP_NO = :3
  
  UNION ALL
  
  SELECT PICTURE
  FROM ADMAIN.ATA020
  WHERE EMP_NO = :4
  
  UNION ALL
  
  SELECT PICTURE
  FROM ADMAIN.SANHAK_LIB
  WHERE EMP_NO = :5
)
WHERE ROWNUM = 1